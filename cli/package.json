{
  "name": "@lbx/partner-cli",
  "version": "0.2.0",
  "description": "Command-line client for the LBX Partner API",
  "type": "module",
  "bin": { "lbx": "./bin/lbx.ts" },
  "module": "src/cli.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lbx": "bun run bin/lbx.ts"
  },
  "devDependencies": {
    "@types/bun": "1.4.1",
    "typescript": "^5.6.0"
  },
  "engines": { "bun": ">=1.2.0" }
}
